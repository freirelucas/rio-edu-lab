"""Per-paper notebook template modules.

Each module is named after the catalog paper id (kebab-case → snake_case) and
exports `build(paper: dict, prov: dict, **kwargs) -> nbformat.NotebookNode`.
"""
